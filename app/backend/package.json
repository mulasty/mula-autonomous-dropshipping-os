{
  "name": "mula-autonomous-dropshipping-os-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Thin backend skeleton for Mula Group Autonomous Dropshipping OS.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "fastify": "^5.3.3",
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.15.2",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
